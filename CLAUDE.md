# かんちゃんのポートフォリオサイト（Claude Code向けメモ）

動画編集者「かんちゃん」の個人ポートフォリオ。静的サイト（HTML/CSS/JS、ビルドなし）で、GitHub Pages で公開する。

## 守ること

- 文章と作品データは `content.js` に集約する。HTMLに文章を直書きしない（固定の見出しやラベルは除く）
- ビルドツール、フレームワーク、npmパッケージを入れない。`index.html` をダブルクリックで開いて動くこと（`fetch` でJSONを読む方式は不可）
- 外部依存は Google Fonts（Zen Kaku Gothic New）のみ
- サイト内の文章に波ダッシュ「〜」を使わない。範囲は「から」で書く
- 表示名は「かんちゃん」（ひらがな）。実名、住所、生年月日、大学名は載せない
- デザインは白基調。カード状の影、英字大文字の見出しラベル、スクロールで順に現れるアニメーション、見出し一語だけの色変更、リンク末尾の「→」は使わない
- 色は `style.css` の `:root` の変数だけを使う。accent（青）はリンク、ボタン、フォーカスのみ
- 動画は `youtube-nocookie.com` の軽量埋め込み（クリックで iframe に差し替え）

## 確認方法

- 公開前に PowerShell で `./tools/check.ps1` を実行する（仮の文言の残り、ID未設定、波ダッシュなどを検出）
- 幅375pxと1280pxで横スクロールが出ないことを確認する

## 質問の仕方

不明点は推測で進めず、本人に一つずつ順番に質問する。
